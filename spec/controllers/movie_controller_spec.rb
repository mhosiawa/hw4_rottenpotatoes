require 'spec_helper'

describe MoviesController do
	describe 'searching movies with the same director' do
		it 'should call a model method to search for movies'
		it 'should select a template for rendering'
		it 'should make the search results available to rendered template'
	end
end
